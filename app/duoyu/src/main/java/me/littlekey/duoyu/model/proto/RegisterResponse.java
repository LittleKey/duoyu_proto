// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: business/account.proto at 40:1
package me.littlekey.duoyu.model.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireEnum;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Boolean;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class RegisterResponse extends Message<RegisterResponse, RegisterResponse.Builder> {
  public static final ProtoAdapter<RegisterResponse> ADAPTER = new ProtoAdapter_RegisterResponse();

  private static final long serialVersionUID = 0L;

  public static final Boolean DEFAULT_SUCCESS = false;

  public static final Errno DEFAULT_ERRNO = Errno.UNKNOWN;

  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#BOOL"
  )
  public final Boolean success;

  @WireField(
      tag = 2,
      adapter = "me.littlekey.duoyu.model.proto.RegisterResponse$Errno#ADAPTER"
  )
  public final Errno errno;

  @WireField(
      tag = 3,
      adapter = "me.littlekey.duoyu.model.proto.User#ADAPTER"
  )
  public final User user;

  public RegisterResponse(Boolean success, Errno errno, User user) {
    this(success, errno, user, ByteString.EMPTY);
  }

  public RegisterResponse(Boolean success, Errno errno, User user, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.success = success;
    this.errno = errno;
    this.user = user;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.success = success;
    builder.errno = errno;
    builder.user = user;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof RegisterResponse)) return false;
    RegisterResponse o = (RegisterResponse) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(success, o.success)
        && Internal.equals(errno, o.errno)
        && Internal.equals(user, o.user);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (success != null ? success.hashCode() : 0);
      result = result * 37 + (errno != null ? errno.hashCode() : 0);
      result = result * 37 + (user != null ? user.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (success != null) builder.append(", success=").append(success);
    if (errno != null) builder.append(", errno=").append(errno);
    if (user != null) builder.append(", user=").append(user);
    return builder.replace(0, 2, "RegisterResponse{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RegisterResponse, Builder> {
    public Boolean success;

    public Errno errno;

    public User user;

    public Builder() {
    }

    public Builder success(Boolean success) {
      this.success = success;
      return this;
    }

    public Builder errno(Errno errno) {
      this.errno = errno;
      return this;
    }

    public Builder user(User user) {
      this.user = user;
      return this;
    }

    @Override
    public RegisterResponse build() {
      return new RegisterResponse(success, errno, user, super.buildUnknownFields());
    }
  }

  public enum Errno implements WireEnum {
    UNKNOWN(0),

    EMAIL_IN_USE(1),

    INVALID_EMAIL_CODE(2),

    LANGUAGE_EMPTY(3),

    NICKNAME_EMPTY(4);

    public static final ProtoAdapter<Errno> ADAPTER = ProtoAdapter.newEnumAdapter(Errno.class);

    private final int value;

    Errno(int value) {
      this.value = value;
    }

    /**
     * Return the constant for {@code value} or null.
     */
    public static Errno fromValue(int value) {
      switch (value) {
        case 0: return UNKNOWN;
        case 1: return EMAIL_IN_USE;
        case 2: return INVALID_EMAIL_CODE;
        case 3: return LANGUAGE_EMPTY;
        case 4: return NICKNAME_EMPTY;
        default: return null;
      }
    }

    @Override
    public int getValue() {
      return value;
    }
  }

  private static final class ProtoAdapter_RegisterResponse extends ProtoAdapter<RegisterResponse> {
    ProtoAdapter_RegisterResponse() {
      super(FieldEncoding.LENGTH_DELIMITED, RegisterResponse.class);
    }

    @Override
    public int encodedSize(RegisterResponse value) {
      return (value.success != null ? ProtoAdapter.BOOL.encodedSizeWithTag(1, value.success) : 0)
          + (value.errno != null ? Errno.ADAPTER.encodedSizeWithTag(2, value.errno) : 0)
          + (value.user != null ? User.ADAPTER.encodedSizeWithTag(3, value.user) : 0)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, RegisterResponse value) throws IOException {
      if (value.success != null) ProtoAdapter.BOOL.encodeWithTag(writer, 1, value.success);
      if (value.errno != null) Errno.ADAPTER.encodeWithTag(writer, 2, value.errno);
      if (value.user != null) User.ADAPTER.encodeWithTag(writer, 3, value.user);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RegisterResponse decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.success(ProtoAdapter.BOOL.decode(reader)); break;
          case 2: {
            try {
              builder.errno(Errno.ADAPTER.decode(reader));
            } catch (ProtoAdapter.EnumConstantNotFoundException e) {
              builder.addUnknownField(tag, FieldEncoding.VARINT, (long) e.value);
            }
            break;
          }
          case 3: builder.user(User.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public RegisterResponse redact(RegisterResponse value) {
      Builder builder = value.newBuilder();
      if (builder.user != null) builder.user = User.ADAPTER.redact(builder.user);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
